# Steps To Run

```bash
python3.12 -m venv .venv            # create enviroment .venv
source ./.venv/bin/activate         # active the .venv
pip install -r requirements.txt     # install requirements
```
